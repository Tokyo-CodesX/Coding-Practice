#include<stdio.h>
int main(){
int length;
printf("Enter the length of a square: \n");
scanf("%d", &length);
int area= length*length;
printf("The area of the square is %d", area);




    return 0;
}